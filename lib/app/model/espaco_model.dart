class EspacoModel {
  double id;
  String logradouro;
  int numero;
  int cep;
  int capacidadePessoas;

  EspacoModel(
      this.id, this.logradouro, this.numero, this.cep, this.capacidadePessoas);
}